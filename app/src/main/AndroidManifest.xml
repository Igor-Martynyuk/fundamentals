<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.itea.practice.fundamentals">

    <application
        android:name=".FundamentalsApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".start.splash.SplashActivity"
            android:noHistory="true"
            android:theme="@style/AppTheme.Activity.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".start.menu.MenuActivity" />
        <activity android:name=".task.components.ui.PingDashboardActivity" />
        <activity android:name=".task.components.ui.PingHistoryActivity" />

        <service
            android:name=".task.components.data.PingService"
            android:enabled="true"
            android:exported="false" />

        <provider
            android:name=".task.components.data.PingHistoryProvider"
            android:authorities="com.itea.practice.fundamentals.ping"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>