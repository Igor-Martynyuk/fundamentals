<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/spacing_default">

    <TextView
        android:id="@+id/output_index"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginEnd="@dimen/spacing_default"
        android:textSize="14sp"
        tools:text="1." />

    <TextView
        android:id="@+id/output_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/output_index"
        tools:text="17.11.2000 20:73:11"
        tools:textColor="@color/highlight_positive" />

    <TextView
        android:id="@+id/output_duration"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/output_date"
        android:layout_toEndOf="@id/output_index"
        tools:text="Duration: 200 ms" />

</RelativeLayout>